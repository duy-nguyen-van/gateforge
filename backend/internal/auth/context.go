package auth

// EchoContextUserIDKey is the Echo context key for the authenticated user's ID (JWT sub).
const EchoContextUserIDKey = "user_id"

// EchoContextTenantIDKey is the Echo context key for the active tenant ID from JWT.
const EchoContextTenantIDKey = "tenant_id"
